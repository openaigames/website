# Community room release · 2026-09-16

Asset tag: `room-community-20260916-1`.

- Production Worker: `9037584e-8a87-4412-aacc-01dd6de34818`
- Preview Worker: `a39fe1f5-f2ff-4fd8-9466-0924baae64d0`
- Legacy gateway: `dd764081-8778-4a69-8060-c99934cbe1f9`

Includes the room radio, built-in original instrumental tracks, local audio/M3U/JSON and public platform playlist import, mobile sheets, camera pan and cabinet refinements, GitHub account entry, and a bilingual moderation desk. Platform tracks remain subject to each platform's public playback availability.

Validation: build succeeded; 54 automated tests passed. Local desktop/mobile UI checked; an isolated database was used to exercise approval, archiving and review history. Production music was played through the browser and advanced normally with no media error. Both public origins returned the same board, intake and catalog payloads; both served the release asset tag. The seven existing submissions were retained as approved. The legacy gateway has no D1 bindings, and its `/admin` redirects to the primary domain.

The OAuth app uses the OpenAIGames logo and exact production/local callbacks. GitHub's sign-in page was verified to show the app correctly. The full authenticated browser round trip remains to be verified: Chrome reported an extension UI blocking automation. Protocol and authorization behavior are covered by the isolated integration tests; this is not evidence that a real browser sign-in completed.

A private database backup was made before schema migration. Intake was temporarily paused during migration, then restored with the matching application release. Never roll back to pre-moderation code with the new schema: that code reads pending records as public. Use a corrective release that retains moderation privacy instead.
